# Supply Chain Intelligence System - Requirements
# Python 3.8+ required

# Core dependencies
pandas>=1.5.0
numpy>=1.20.0
openpyxl>=3.0.0  # For Excel file handling

# AI/ML (FREE - no API key needed)
transformers>=4.30.0
torch>=2.0.0
sentencepiece>=0.1.99  # Required by Flan-T5

# Visualization (optional but recommended)
matplotlib>=3.5.0
seaborn>=0.12.0

# Data processing
scikit-learn>=1.0.0  # Optional: for advanced analytics

# Utilities
tqdm>=4.65.0  # Progress bars
python-dateutil>=2.8.0

# Development dependencies (optional)
pytest>=7.0.0  # For testing
black>=23.0.0  # Code formatting
flake8>=6.0.0  # Linting

# Notes:
# - All packages are free and open-source
# - transformers will download ~900MB model on first run
# - torch can be CPU-only (no GPU needed)
# - Total install size: ~2GB including model
